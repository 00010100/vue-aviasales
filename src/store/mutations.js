export const setSearchId = (state, searchId) => {
  state.searchId = searchId
}

export const setStopSearch = (state, stop) => {
  state.stopSearch = stop
}

export const setTickets = (state, tickets) => {
  state.tickets = tickets
}
